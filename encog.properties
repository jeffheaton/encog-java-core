;This same property file should be shared over ALL Encog projects, both Java and C#.
;Currently, the only purpose is to specify the Encog version number.
;
;When increasing the Encog version number, modify this file, and copy to all other
;Encog projects, i.e. core, examples, workbench, C# version, Silverlight version etc.
;You should also update the Encog version number inside of the Encog class.
encog.version=2.2.0